# Outputs

Runtime artifacts produced by evaluation runs:

- execution logs
- JSON reports
- YAAM memory dumps
- traces/exports (e.g., Arize Phoenix)

This directory is gitignored by default (except this README).

