# Tools (Skill Factory)

This directory hosts **Python skills** imported from the Skill Factory.

Conventions:

- Skills should be importable Python modules/packages.
- `tools/registry/` is reserved for discovery/registration helpers used by the agentic runtime.

