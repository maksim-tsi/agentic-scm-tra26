# Benchmark Data

This directory stores the **golden** SCM-Cert-Bench tasks (expected in JSON/JSONL form).

Recommended layout (to be populated):

- `tasks.jsonl` — one task per line
- `metadata.info` — template/schema notes for Cognitive Task Templates (CTT)

